% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PaletteCode.R
\name{era.brewer}
\alias{era.brewer}
\title{Generate a color palette}
\usage{
era.brewer(
  palette_name,
  n,
  type = c("discrete", "continuous"),
  direction = c(1, -1),
  override_order = FALSE,
  return_hex = FALSE
)
}
\arguments{
\item{palette_name}{Character. Name of the palette; must be one of
\code{names(EraPalettes)}.}

\item{n}{Integer. Number of colors to return. Defaults to the full palette
length.}

\item{type}{One of \code{"discrete"} or \code{"continuous"}. If omitted,
chosen automatically: \code{"continuous"} when \code{n} exceeds the
palette length, otherwise \code{"discrete"}.}

\item{direction}{\code{1} for the standard order, \code{-1} for reversed.}

\item{override_order}{Logical. If \code{TRUE}, return colors in their stored
order rather than the curated order-of-use for the requested \code{n}.}

\item{return_hex}{Logical. If \code{TRUE}, also prints the hex codes.}
}
\value{
An object of class \code{"palette"}: a character vector of hex
  codes with the palette name stored as an attribute.
}
\description{
Returns a vector of colors drawn from one of the palettes in
\code{\link{EraPalettes}}. Supports both discrete (curated subsets) and
continuous (interpolated) palettes.
}
\examples{
# Discrete palette using the curated order-of-use
print(era.brewer("Lover2", n = 3))

# Continuous interpolation when n exceeds the stored palette length
print(era.brewer("Showgirl2", n = 50, type = "continuous"))

# Reverse direction
print(era.brewer("Fearless", direction = -1))

# Plug into a ggplot2 manual scale
library(ggplot2)
ggplot(iris, aes(Sepal.Length, Sepal.Width, color = Species)) +
  geom_point() +
  scale_color_manual(values = era.brewer("Lover2", n = 3))
}
